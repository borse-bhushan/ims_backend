{
    "ENV": "LOCAL",
    "PROJECT_NAME": "PROJECT_NAME",
    "ALLOWED_HOSTS": [
        "Add the hosts or add * to allow all"
    ],
    "DISABLE_PRINT": false,
    "DATABASES": {
        "default": {
            "ENGINE": "django.db.backends.sqlite3",
            "NAME": "db.sqlite3"
        }
    }
}